<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:textColorPrimary">@color/colorText</item>
        <item name="fontFamily">@font/antinoou</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColorSecondary">@color/colorText</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="android:textViewStyle">@style/TransparentTextView</item>
    </style>

    <style name="TransparentTextView" parent="Widget.AppCompat.TextView">
        <item name="background">@android:color/transparent</item>
    </style>

    <style name="main_Button" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:textSize">18sp</item>
        <item name="fontFamily">@font/antinoou</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="menu_button" parent="Widget.AppCompat.Button.Colored">
        <item name="android:textSize">18sp</item>
        <item name="fontFamily">@font/antinoou</item>
        <item name="android:textStyle">bold</item>
        <item name="backgroundTint">#11ffffff</item>
        <item name="android:drawableStart">@drawable/ic_action_lotus</item>
        <item name="android:drawableEnd">@drawable/ic_action_lotus</item>
    </style>


</resources>
